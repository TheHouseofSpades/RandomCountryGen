public class RandomWorld{
  public double makeWealth(double w){
    if(w > 0) return w;
    int max = 16768100;
    int min = 40;
    double mean = 363448.69;
    int posneg = (int)(Math.random());
    if(posneg == 0){
     return mean - (Math.random() * 36408.89); 
    }
    else if(posneg == 1){
     return mean + (Math.random() * 1314651.31); 
    }
    return 0;
    
  }
  
}